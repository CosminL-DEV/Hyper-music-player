-- Hyper Database
-- Version 1.0

CREATE DATABASE IF NOT EXISTS hyper;
USE hyper;

--
-- Creación de las tablas
--

DROP TABLE IF EXISTS users;
CREATE TABLE users (
  profile_pic VARCHAR(150),
  username VARCHAR(16) NOT NULL,
  password VARCHAR(16) NOT NULL,
  PRIMARY KEY (username)
);

DROP TABLE IF EXISTS artist;
CREATE TABLE artist (
  artist_id SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT,
  profile_pic VARCHAR(150) NOT NULL,
  name VARCHAR(16) NOT NULL,
  PRIMARY KEY (artist_id, name)
);

DROP TABLE IF EXISTS album;
CREATE TABLE album (
  album_id SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT,
  name VARCHAR(35) NOT NULL,
  songs JSON
);

DROP TABLE IF EXISTS playlist;
CREATE TABLE playlist (
  playlist_id SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT,
  picture VARCHAR(150),
  privacity VARCHAR(7), -- private, public, shared

  -- songs JSON
);

-- PLAN: Crear otra tabla con song_id, fecha add, user added, relacion con la playlist. Y despues relacionar todo en una playlist.

DROP TABLE IF EXISTS song;
CREATE TABLE song (
  song_id SMALLINT UNSIGNED NOT NULL AUTO_INCREMENT,
);



--
-- Inserción de datos
--

INSERT INTO users(username,password) VALUES ('cosmin','prueba');